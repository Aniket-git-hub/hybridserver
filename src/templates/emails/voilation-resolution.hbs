<!-- src/templates/emails/violation-resolution.hbs -->
<html>
	<head>
		<meta charset="utf-8" />
		<title>Violation Successfully Resolved</title>
		<style>
			body { font-family: Arial, sans-serif; line-height: 1.6; color:
			#333; margin: 0; padding: 0; } .container { max-width: 600px;
			margin: 0 auto; padding: 20px; } .header { background-color:
			#4caf50; padding: 20px; text-align: center; color: white; } .content
			{ padding: 20px; background-color: #f9f9f9; } .button { display:
			inline-block; padding: 12px 24px; background-color: #4a86e8; color:
			white; text-decoration: none; border-radius: 4px; margin: 20px 0;
			font-weight: bold; } .footer { text-align: center; margin-top: 20px;
			font-size: 12px; color: #777; } .success-box { background-color:
			#e8f5e9; padding: 15px; margin: 15px 0; border-radius: 5px;
			border-left: 4px solid #4caf50; } .resolution-details { margin: 15px
			0; } .detail-row { display: flex; margin-bottom: 8px; }
			.detail-label { font-weight: bold; width: 150px; }
			.resolution-status { color: #4caf50; font-weight: bold; }
		</style>
	</head>
	<body>
		<div class="container">
			<div class="header">
				<h1>Violation Successfully Resolved</h1>
			</div>
			<div class="content">
				<p>Hello {{name}},</p>
				<p>We're pleased to inform you that the following traffic
					violation has been successfully resolved:</p>

				<div class="success-box">
					<h3>Resolution Details:</h3>
					<div class="resolution-details">
						<div class="detail-row">
							<span class="detail-label">Challan Number:</span>
							<span>{{challanNumber}}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Vehicle:</span>
							<span>{{vehicleNumber}} - {{vehicleModel}}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Violation Type:</span>
							<span>{{violationType}}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Fine Amount:</span>
							<span>{{fineAmount}}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Resolution Date:</span>
							<span>{{resolutionDate}}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Resolution Method:</span>
							<span>{{resolutionMethod}}</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Status:</span>
							<span class="resolution-status">RESOLVED</span>
						</div>
						<div class="detail-row">
							<span class="detail-label">Receipt Number:</span>
							<span>{{receiptNumber}}</span>
						</div>
					</div>
				</div>

				<p>The resolution has been recorded in your system, and your
					vehicle compliance status has been updated accordingly.</p>

				<div style="text-align: center;">
					<a href="{{violationHistoryUrl}}" class="button">View
						Violation History</a>
				</div>

				<p>Thank you for promptly addressing this violation. Maintaining
					a clean compliance record helps avoid unnecessary penalties
					and keeps your fleet operations running smoothly.</p>

				<p>Best regards,<br />The {{appName}} Team</p>
			</div>
			<div class="footer">
				<p>&copy; {{currentYear}} {{appName}}. All rights reserved.</p>
				<p>{{companyAddress}}</p>
				<p><a href="{{unsubscribeUrl}}">Manage notification preferences</a></p>
			</div>
		</div>
	</body>
</html>